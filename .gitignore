# Build output
/bin/
/dist/
*.exe
*.out
# Compiled binaries built in place by `go build -o <name>`
/dsoserver
/dsoclient
/dsotool

# Server logs and local run scripts
*.log
/server.bat

# Runtime data (keys, config, sqlite, packet dumps)
/data/

# Go
/vendor/
*.test
*.prof

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Reference material (kept out of the new project's history)
/ref/

# Local server config (contains your LAN address).
/dso.env
